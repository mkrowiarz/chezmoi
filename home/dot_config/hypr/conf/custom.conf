# Custom Configuration
# Add your personal overrides here
# This file is for local machine-specific tweaks

# Example:
# monitor = DP-1, 2560x1440@144, 0x0, 1
# bind = $mainMod, G, exec, google-chrome-stable

# Audio: move all streams to default sink (useful after connecting Bluetooth)
bind = $mainMod SHIFT, A, exec, ~/.local/bin/audio-to-default
