[Unit]
Description=Caddy Docker Proxy
After=podman.socket
Requires=podman.socket

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=%h/.local/share/caddy-proxy
ExecStart=podman-compose up -d
ExecStop=podman-compose down
Restart=on-failure

[Install]
WantedBy=default.target
